box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.12
        theme: casper
        flags: --buildDrafts=true

deploy:
  steps:
    - moos3/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: guthur.net
        basedir: public

